export type InfoCharacters = {
  count: number;
  pages: number;
  next: string;
  prev: string;
};

export type InfoEpisodes = {
  count: number;
  pages: number;
  next: string;
  prev: string;
};

export type InfoLocations = {
  count: number;
  pages: number;
  next: string;
  prev: string;
};
